<div class="profile-tabs">
  <ul  role="tablist">
    <% tabs.each do |name, args| %>
      <% css_class = name == active ? 'active' : '' %>
      <li class="<%=css_class %>"><%= link_to t(args[0]), args[1]%></li>
    <% end %>
  </ul>
</div>